<view class="page">
  <button bindtap="onLogin" loading="{{loading}}" disabled="{{loading}}">
    {{loading ? "登录中" : "微信登录"}}
  </button>
  <view wx:if="{{errorMessage}}" class="error">{{errorMessage}}</view>
</view>
